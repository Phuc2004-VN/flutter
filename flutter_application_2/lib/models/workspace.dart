class Workspace {
  final String id;
  final String name;

  Workspace({
    required this.id,
    required this.name,
  });

  // Optional: Add toJson and fromJson methods later if needed for persistence
} 